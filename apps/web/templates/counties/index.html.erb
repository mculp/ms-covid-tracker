<div class="container">
  <table class="table table-bordered table-hover sortable-theme-bootstrap" data-sortable>
    <caption style="caption-side: top">Updated <%= updated %></caption>
    <thead>
      <tr>
        <th>County</th>
        <th>Total Cases</th>
        <th id="new-cases-header" data-sorted="true" data-sorted-direction="descending">New Cases</th>
        <th>Total Deaths</th>
        <th>New Deaths</th>
      </tr>
    </thead>

    <tbody>
    <% counties.each do |county| %>
      <tr>
        <td>
          <a href="/counties/<%= county.name %>">
            <h6><%= county.name %></h6>
          </a>
        </td>
        <td><h6><%= county.county_updates.first.cases %><h6></td>
        <td>
          <h6><%= county.county_updates.first.new_cases %></h6>
          <span class="badge <%= badge_class(county.county_updates.first.new_cases_percent_change) %>">
            <%= county.county_updates.first.new_cases_percent_change %>%
          </span>
        </td>
        <td><h6><%= county.county_updates.first.deaths %></h6></td>
        <td><h6><%= county.county_updates.first.new_deaths %><h6></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
